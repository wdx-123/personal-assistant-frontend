I will implement the "Team Management" module restructure as requested. This involves creating new views for "My Team" and "Team Members" and updating the navigation.

**Plan Details:**

1. **Create New View Components**:
   - **`src/views/Console/Team/MyTeam.vue`**:
     - **Layout**: Card-based grid layout using mock data.

     - **Features**: Display team cards with "Join Team", "Create Team" buttons. Each card will show team details and actions (Edit, Delete, Switch).

   - **`src/views/Console/Team/TeamMembers.vue`**:
     - **Layout**: Table layout with search and header summary.

     - **Features**: List members with columns (Index, Avatar, Username, Role, Email, Status). Actions for Edit and Delete.

2. **Update Routing (`src/router/index.ts`)**:
   - Replace the existing `/console/team` route.

   - Add a new route structure:
     - Parent: `/console/team` (redirects to `/console/team/list`)

     - Child: `list` -> `MyTeam.vue` (Name: "My Team")

     - Child: `members` -> `TeamMembers.vue` (Name: "Team Members")

3. **Update Sidebar Navigation (`src/components/layout/ConsoleSidebar.vue`)**:
   - **Add**: A new "Team Management" (团队管理) menu group.

   - **Add Sub-menus**: "My Team" (我的团队) and "Team Members" (团队成员).

   - **Remove**: The existing "Personnel Management" (人员管理), "Organization Management" (组织管理), and the old "My Team" (我的团队) menu items, as they are being merged into this new structure.

4. **Clean Up**:
   - Delete the existing placeholder file `src/views/Console/Team.vue`.
